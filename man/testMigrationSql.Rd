% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_migration.R
\name{testMigrationSql}
\alias{testMigrationSql}
\title{Test that a migration SQL file correctly transforms a module schema}
\usage{
testMigrationSql(
  module,
  migrationFile,
  fromVersion = "latest",
  toDefinition,
  modulesRoot = resolvePackageDir("modules"),
  databaseSchema = "main"
)
}
\arguments{
\item{module}{Module name as it appears under \code{modulesRoot}
(e.g.\ \code{"CohortGenerator"}).}

\item{migrationFile}{Path to the \code{migration.sql} file to test.}

\item{fromVersion}{Version string to start from (e.g.\ \code{"v0.1.0"}) or
\code{"latest"} (default) to use the highest registered version.}

\item{toDefinition}{The expected post-migration schema. Accepts:
\itemize{
\item A version string such as \code{"v1.0.0"} (looked up in \code{modulesRoot}).
\item A path to a \code{definition.yaml} file (useful for unregistered versions).
\item A parsed list as returned by \code{yaml::read_yaml()}.
}}

\item{modulesRoot}{Path to the package modules directory.}

\item{databaseSchema}{Schema name used when rendering the OHDSI SQL.
Defaults to \code{"main"} (the DuckDB default schema).}
}
\value{
Invisibly returns \code{TRUE} when the migration test passes.
}
\description{
Creates an in-memory DuckDB database, builds the starting schema from the
\code{fromVersion} YAML definition, applies the migration SQL, and then verifies
that the resulting schema contains all tables and columns declared in
\code{toDefinition}. Throws an informative error on any mismatch; returns
\code{invisible(TRUE)} on success.
}
\details{
The migration SQL must be in OHDSI SQL format (i.e.\ parameterized with
\verb{@database_schema}) so it can be rendered by \code{SqlRender::render()}.
}
