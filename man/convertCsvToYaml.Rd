% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_csv.R
\name{convertCsvToYaml}
\alias{convertCsvToYaml}
\title{Convert a legacy CSV results data model specification to a YAML definition}
\usage{
convertCsvToYaml(
  csvFile,
  outputDir,
  moduleName,
  prefix,
  version = "v1.0.0",
  addPrefix = NULL
)
}
\arguments{
\item{csvFile}{Path to the input CSV file.}

\item{outputDir}{Root output directory. The definition is written to
\code{outputDir/moduleName/version/definition.yaml}.}

\item{moduleName}{Name of the module (e.g.\ \code{"CohortMethod"}).}

\item{prefix}{Table name prefix including trailing underscore
(e.g.\ \code{"cm_"}).}

\item{version}{Target module version string (default \code{"v1.0.0"}).}

\item{addPrefix}{Whether to prepend \code{prefix} to table names that do not
already carry it. \code{NULL} (default) auto-detects based on the table names
found in the CSV.}
}
\value{
Invisibly returns the path to the written YAML file.
}
\description{
Reads a CSV file describing a module's results data model and writes a
\code{definition.yaml} compatible with the HADES module schema to
\code{outputDir/moduleName/version/definition.yaml}.
}
\details{
The CSV must contain at least columns for table name, column/field name, and
data type. Optional columns for description, primary key flag, and
deprecation flag are used when present. Column names are matched
case-insensitively against common variants (e.g.\ \code{table_name},
\code{column_name}, \code{data_type}, \code{description}, \code{primary_key}).
}
