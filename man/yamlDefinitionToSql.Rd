% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migration_engine.R
\name{yamlDefinitionToSql}
\alias{yamlDefinitionToSql}
\title{Convert a module YAML definition to OHDSI SQL CREATE TABLE statements}
\usage{
yamlDefinitionToSql(
  definition,
  databaseSchema = "@database_schema",
  additionalTables = c("cg_cohort_definition", "database_meta_data")
)
}
\arguments{
\item{definition}{Either a path to a \code{definition.yaml} file or a list as
returned by \code{yaml::read_yaml()}.}

\item{databaseSchema}{Schema parameter to embed. Defaults to the literal
\code{"@database_schema"} for deferred rendering via \code{SqlRender::render()}.}

\item{additionalTables}{Character vector of table names from other modules
that may appear in foreign-key references. Defaults to the two standard
cross-module tables.}
}
\value{
A single character string of OHDSI SQL \verb{CREATE TABLE} statements.
}
\description{
Reads a HADES module definition (file path or parsed list) and produces
OHDSI SQL \verb{CREATE TABLE} statements parameterized with \verb{@database_schema}.
Render the returned SQL with \code{SqlRender::render(sql, database_schema = "mySchema")}
before execution.
}
